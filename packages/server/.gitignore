node_modules

dist
coverage
*.tsbuildinfo

.DS_Store

# Declarations belong in dist/. These only ever appear in src/ when a build is
# misconfigured, and once committed nothing regenerates them — so they drift
# silently the moment an exported type changes.
src/**/*.d.ts
src/**/*.d.ts.map
