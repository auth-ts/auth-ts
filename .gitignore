node_modules
.nx
coverage
.DS_Store

# `nx keygen cli` runs the CLI in its own package, so its output lands here
packages/cli/jwks.json

.env
.env.*
!.env.example

.claude/worktrees
.claude/settings.local.json
