---
title: CLI
description: bun x @auth-ts/cli keygen — the signing key, the secret, and jwks.json.
---

```bash
bun x @auth-ts/cli keygen --out public
```

`keygen` generates a signing key and a server secret, prints them as the two
`.env` lines `@auth-ts/server` reads, and writes the public key set to
`jwks.json`.

`--out` says where that file goes, and defaults to the working directory.
Frameworks that serve a folder verbatim — TanStack Start, Next.js, Vite, Nuxt —
want `--out public`, which is what puts it at `<origin>/jwks.json`.

`--dry` writes nothing and prints the key set as a third line instead, for
looking before committing to a file.

```text
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
AUTH_SECRET="..."
```

Copy them into your `.env`. They are the whole of stdout — the path it wrote
goes to stderr — so they pipe cleanly, though into a file you already keep
rather than appended blind: run twice and you have two `JWT_PRIVATE_KEY` lines,
and which one wins is up to whichever loader reads it.

The private key is quoted with its line breaks as literal `\n`, which dotenv,
Bun, Vite, and Next all expand.

## `jwks.json`

The public half of the key, as a JWKS document. A framework with a public
folder — TanStack Start, Next.js, Vite — serves it at `/jwks.json`, which is the
URL you give Neon, or anything else that verifies against a JWKS. Its `kid` is
the key's thumbprint, the same one the server stamps on every token header.

The file belongs to the key: running `keygen` again writes a new key and a new
file, and the two are deployed together. See [rotating keys](/docs/guides/key-rotation).

No public folder? Import the file and pass it to `createAuthServer` as
`jwks: { json }`; it is then served at `/api/auth/jwks`. If it is hosted
somewhere else, `jwks: { url }` tells the discovery document where.

## Options

| Flag | Default | |
| --- | --- | --- |
| `--alg RS256\|ES256` | `RS256` | The signing algorithm. Match `jwt.alg` on the server. |
